import React from 'react'

const StarRating = ({ rating }) => {
    // const ratingStar = Array.from((_, index) => {

    // })

    // console.log(rating.rate, "rating");


    return (
        <div>StarRating</div>
    )
}

export default StarRating